---
title: '高级统计方法 第一轮学习 Chapter 6'
published: 2025-11-23
description: '高级统计方法 第一轮学习 Chapter 6 线性模型选择与正则化'
image: ''
tags: ['Statistical Learning']
category: 'Study'
draft: false
series: 'Maths'
---

# 高级统计方法 第一轮学习 Chapter 6 线性模型选择与正则化

## 准确率与解释力

### 0.1 最小二乘的预测准确率（prediction accuracy）

#### 最小二乘的预测准确率（prediction accuracy）

- 若响应变量和预测变量的真实关系近似线性，则最小二乘的偏差较低。

- 若观测个数 $n$ 远大于预测变量个数 $p$，则最小二乘的方差也较低。

- 若观测个数 $n$ 不满足远大于预测变量个数 $p$，则使用最小二乘可能导致过拟合，方差较大。

- 若观测个数 $n$ 小于预测变量个数 $p$，则最小二乘得到的系数估计结果不唯一：此时方差无穷大，无法使用最小二乘法。

### 0.2 最小二乘的模型解释力（model interpretability）

#### 最小二乘的模型解释力（model interpretability）

- 在多元回归模型中，常常存在一个或多个预测变量与响应变量不存在线性关系的情况，包括一些增加了模型的复杂性、却与模型无关的变量。

- 通过去除不相关的特征，将无关变量的系数设置为 $0$，并移除这些无关变量（去不相关）可得到一个更容易解释的模型，但最小二乘法很难将系数置为 $0$。

### 0.3 改进方法

#### 子集选择

从 $p$ 个预测变量中挑选出与响应变量相关的变量形成子集，然后再对缩减的变量集合使用最小二乘方法。

#### 压缩估计（正则化）

基于全部 $p$ 个预测变量进行模型的拟合，但与最小二乘方法相比，该方法自动将估计系数往零的方向进行压缩。通过系数缩减(又称正则化) 减少方差，还可以用于变量选择。

#### 降维法

将 $p$ 维预测变量投影至 $M$ 维子空间中（$M < p$）。这通常通过计算这 $p$ 个变量的 $M$ 种不同的线性组合或称投影来实现。将这个不同的投影作为预测变量，再使用最小二乘法拟合线性回归模型。

目标：

1. 预测准确率：$n >> p$

2. 模型解释力：筛选无关变量

## 子集选择

### 1.1 最优子集选择

#### 最优子集选择

1. 记不含预测变量的零模型为 $M_0$，只用于估计各观测的样本均值。

2. 对每一个 $k = 1, 2, \ldots, p$：

   1. 拟合 $\binom{p}{k}$ 个包含 $k$ 个预测变量的子集模型。

   2. 在这 $\binom{p}{k}$ 个子集模型中，选择 RSS 最小或 $R^2$ 最大的那个子集模型，记为 $M_k$。

3. 根据交叉验证模型误差、$C_p$（AIC）、BIC 或者调整 $R^2$ 从 $M_0, \ldots, M_p$ 个模型中选择出一个最优模型。

低 RSS 及高 $R^2$ 表明模型的训练误差低，随着预测变量的增加，RSS 和 $R^2$ 总是变的更好。

但我们的目标是选择一个测试误差低的模型，所以，不能使用训练集的 RSS 和 $R^2$ 进行模型选择。需要估计测试误差，然后据此进行选择。

可以考虑两种方法：
1. 第五章的方法：交叉验证或者验证集等方法，直接估计测试误差；
2. 对训练误差进行调整，直接估计测试误差：如 $C_p$, AIC, BIC, 调整 $R^2$ 等统计量

#### 拓展

在用于逻辑斯蒂回归时，算法步骤2中应当使用偏差（deviance）替代原先的 RSS 对模型进行选择，偏差与 RSS 作用相似，但适用范围更广。

偏差的定义为：

$$
D = -2 \Big[ \ell(\hat{\theta}; y) - \ell(\text{饱和模型}; y) \Big]
$$

$$
D \approx -2 \cdot \ell(\hat{\theta}; y) \quad \text{（近似成立）}
$$

此处，饱和模型就是“理论上能完美拟合所有观测值的模型”。由于在比较不同模型时，饱和模型的对数似然值是一个常数，所以在比较不同模型的偏差时，可以忽略该常数项。

#### 缺点

- 最优子集选择方法简单直观，但计算效率不高，当 $p>40$，该方法不具计算可行性。
- 随着搜索空间增大，通过此方法找到的模型对新数据不具备很好的预测能力。
- 从一个巨大搜索空间得到的模型通常会有过拟合和系数估计方差高的问题（含义与上述类似）。

### 1.2 向前逐步选择

#### 向前逐步选择

1. 记不含预测变量的零模型为 $M_0$。

2. 对每一个 $k = 0, 1, \ldots, p - 1$：

   1. 在当前模型 $M_k$ 的基础上，考虑将每一个剩余的预测变量加入到模型中，一共有 $p - k$ 个候选模型。

   2. 在 $p - k$ 个候选模型中，选择一个 RSS 最小或 $R^2$ 最大的模型作为最有模型，记为 $M_{k+1}$。

3. 根据交叉验证模型误差、$C_p$（AIC）、BIC 或者调整 $R^2$ 从 $M_0, \ldots, M_p$ 个模型中选择出一个最优模型。

向前逐步选择在运算效率上优于最佳子集选择，仅需要搜索 $1 + (p - 0) + (p - 1) + \ldots + 1 = \frac{p(p+1)}{2} + 1$ 个模型。但该方法产生了一系列的嵌套模型（一定包含前期已选择的变量），无法保证找到的模型是所有 $2^p$ 个模型中最优的。

### 1.3 向后逐步选择

#### 向后逐步选择

1. 记包含所有 $p$ 个预测变量的模型为 $M_p$。

2. 对每一个 $k = p, p - 1, \ldots, 1$：

   1. 在当前模型 $M_k$ 的基础上，考虑将每一个预测变量从模型中剔除，一共有 $k$ 个候选模型。

   2. 在 $k$ 个候选模型中，选择一个 RSS 最小或 $R^2$ 最大的模型作为最有模型，记为 $M_{k-1}$。

3. 根据交叉验证模型误差、$C_p$（AIC）、BIC 或者调整 $R^2$ 从 $M_0, \ldots, M_p$ 个模型中选择出一个最优模型。

向后逐步选择在运算效率上也优于最佳子集选择，仅需要搜索 $1 + p + (p - 1) + \ldots + 1 = \frac{p(p+1)}{2} + 1$ 个模型。但该方法也产生了一系列的嵌套模型（一定包含前期已选择的变量），无法保证找到的模型是所有 $2^p$ 个模型中最优的。

需满足样本量 $n$ 大于变量个数 $p$（从而保证全模型可以被拟合）的条件。

而相应的，向前逐步选择则不需要满足该条件。因此，当 $p$ 非常大的时候，向前逐步选择是唯一可行的方法（最优子集法和向后逐步选择均不适用）。

### 如何选择最优模型？——调整训练误差

#### 选择最优模型

包含所有预测变量的模型总是具有最小的 RSS 和最大的 $R^2$，他们只代表了训练误差，不代表泛化能力。

但我们需要找到具有最小测试误差的模型，而训练误差很多时候是测试误差的一个较差的估计。

所以，并不能只使用训练误差（RSS 和 $R^2$）对包含不同个数预测变量的模型进行模型选择。

所以，为了达到基于测试误差选择最优模型（基本原则）的目的，需要估计测试误差（即：下面的方法是估计某类模型的测试误差的方法），通常有两种方法: