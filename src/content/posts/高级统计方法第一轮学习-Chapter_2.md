---
title: '高级统计方法第一轮学习 Chapter 2'
published: 2025-11-18
description: '高级统计方法第一轮学习 Chapter 2'
image: ''
tags: ['Statistical Learning']
category: 'Study'
draft: false
series: 'Maths'
---

# 高级统计方法 第一轮学习 Chapter 2 统计学习

## 一、什么是统计学习？

### 1.1 基本假设

#### 统计学习

对于任意任务，我们都可以假设模型：

$$
Y = f(X) + \epsilon
$$

其中：

$\epsilon$：随机误差项，与 $X$ 独立，服从均值为 0 的某种分布。

$f$：固定但未知的目标函数，$f$ 表达了 $X$ 提供给 $Y$ 的系统信息。

估计 $f$ 的主要目的：

- 有一个好的 $f$，可以在新点 $X = x$ 上预测 $Y$。

- 可以理解 $X = (X_1, X_2, \ldots, X_p)$ 在解释 $Y$ 时哪些是重要元素，而哪些是不相关的。（某种程度上由系数是否为零反映）

- 根据 $f$ 的复杂性，能够理解 $X$ 的每个分量 $X_j$ 如何影响 $Y$。（某种程度上由系数的正负和大小共同反映，也即“正向”、“反向”、“大小”的影响）

### 1.2 我们的假设成立么

#### 估计 $f$

我们可以认为误差源自 $\epsilon$，**存在理想**的 $f(x)$，且 $f(x) = E(Y | X = x)$，称为**回归函数**。

该理想的 $f(x)$ 即为我们要拟合的目标函数。

换言之，在理想情况下：

$$
f(x) = G(Y | X = x)
$$

是使函数 $g$ 在所有 $X = x$ 点上的 $E[(Y − g(X))^2|X = x]$ 最小化的函数。

- 只有 $g(x)$ 取均值的时候可以使得差值平方和最小

- 证明：

  $$
  \begin{aligned}
  E[(Y - g(X))^2 | X = x] & = E[Y^2 | X = x] - 2g(x)E[Y | X = x] + g(x)^2 \\
  & = E[Y^2 | X = x] - (E[Y | X = x])^2 + (g(x) - E[Y | X = x])^2
  \end{aligned}
  $$

  上式中，$E[Y^2 | X = x] - (E[Y | X = x])^2$ 与 $g(x)$ 无关，因此当且仅当 $g(x) = E[Y | X = x]$ 时，$E[(Y - g(X))^2 | X = x]$ 取得最小值。

若某个点数值很多，那么取均值，作为准备拟合的数值；但若某个点周围的数据点很少或没有，怎么办？

我们可以放宽定义，设：$f(x) = Ave(Y | X \in \mathcal{N}(x))$，其中 $\mathcal{N}(x)$ 是 $x$ 的某个邻域。

对于小的 $p$，最近邻平均是非常好的，即 $p \leq 4$ 和大的 $N$。当 $p$ 很大时，最近邻方法可能很糟糕。（此处 $p$ 是指预测变量的个数，$N$ 是指样本量。）
在高维空间中，最近的邻居往往离我们很远。
- 需要得到 $y_i$ 的 $N$ 值的一个合理的部分来取平均，以降低方差。
- 高维中 $10\%$ 的邻域不再是局部的，因此不能通过局部平均来估计 $E(Y|X = X)$。

实际情况是，我们并不知道 $f$ 的真实形式，因此需要通过数据来估计，估计的函数记为 $\hat{f}$。

对于 $f(x)$ 的任何估计 $\hat{f}(x)$，其预测值计算为 $\hat{Y} = \hat{f}(X)$。

此处我们不再考虑误差项 $\epsilon$ 的影响，其误差为 $E[Y - \hat{Y}]^2 = E[f(x) + \epsilon - \hat{f}(X)] ^2 = [f(x) - \hat{f}(X)]^2 + Var(\epsilon)$。

其中，

- $[f(x) - \hat{f}(X)]$：可约误差（reducible error）
- $Var(\epsilon)$：不可约误差（irreducible error）
- $E(Y - \hat{Y})^2$：代表预测量与真实量之间的均方误差（Mean Squared Error，MSE）
- $Var(\epsilon)$：表示误差项 $\epsilon$ 的方差，源自数据中的随机噪声，无法通过任何模型来减少。因为在每个 $X = x$ 处，可能的 $Y$ 值有一个分布，而不是一个确定的值。

不可约误差是精度的上限。

### 1.3 不同的模型

#### 如何估计？

参数模型：把估计 $f$ 的问题简化到估计一组参数。对函数 $f$ 的形式（模型）进行假设，然后用训练数据去拟合/训练模型。这时，只需要估计参数，即可知道函数 $f$。

线性模型是参数模型的一个重要例子：$f _ { L } ( X ) = \beta _ { 0 } + \beta _ { 1 } X _ { 1 } + \beta _ { 2 } X _ { 2 } + \dots \beta _ { p } X _ { p }$
线性模型用 $p + 1$ 个参数表示：$\beta _ { 0 }, \beta _ { 1 }, \dots, \beta _ { p }$

非参数模型：不需要对函数 $f$ 的形式事先做出假设。追求的是接近数据点的估计，估计函数在去粗和光滑后尽可能与更多的数据点接近。非参数模型一般需要更多的训练数据。

非参数模型-薄板样条：使用光滑薄板样条的技术（第7章）拟合，有一个调节参数，又称光滑参数 $\lambda$ $\hat{f}_{S}(\mathtt{education, seniority})$

### 1.4 预测精度与模型可解释性的权衡

通常更喜欢包含更少变量的简单模型，而不是包含所有变量的黑箱预测器。根据任务，推断任务一般喜欢简单和可解释性强的模型（结构限定的模型）；而预测任务时，虽然直觉上感觉光滑度高的“黑盒”可能预测效果更好，但真实情况是欠光滑的模型往往更好（抗“过拟合”）

> 想象你在调样条的平滑参数 λ：
>
> - λ 很大：
>   - 曲线接近直线，非常光滑；
>   - 训练误差大，测试误差也偏大 → 欠拟合。
>
> - λ 很小：
>   - 曲线东扭西扭，每个点都几乎穿过；
>   - 训练误差几乎 0，测试误差很大 → 过拟合。
>
> - λ 在中间某个值：
>   - 曲线有适度的弯曲，有些地方看起来“有点不那么光滑”；
>   - 训练误差不算特别小，但测试误差最小。
>   - 这就是那种“看起来没那么顺眼，但用起来最好”的欠光滑模型。

## 二、评价模型精度

### 2.1 拟合效果检验

薄板样条: thin-plate spline
为拟合薄板样条，一般指定一个 flexibility 水平；
1. 书中译文：光滑度水平
2. 同义译文：柔性水平
3. 正式术语：自由度 degree of freedom
4. 柔性水平增加=光滑度水平增加=自由度增加
5. 此时的光滑度，和直观上“直线/平面更光滑/平滑”不一样
6. 建议大家统一使用“柔性水平”